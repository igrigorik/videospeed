\* Single-defect variant: cell 1 writes the 1.0 baseline in NoOpinion,
\* modeling master before PR #1537. TLC must find an I1 violation whose
\* trace is the repro in issue #1537 (native menu change undone on play).
SPECIFICATION Spec
CONSTANTS
  Speeds = {"one", "v", "w"}
  MaxFight = 2
  BuggyNoOpinionLifecycle = TRUE
  BuggyLifecyclePersist = FALSE
PROPERTIES
  NoOpinionNeverWrites
