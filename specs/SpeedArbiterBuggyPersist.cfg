\* Single-defect variant: cell 6 persists on lifecycle restore, modeling
\* finding F1 (setSpeed step-6 storage write on source:'init'). TLC must
\* find an I2 violation: a site-rule restore clobbering the user's stored
\* speed without any user action.
SPECIFICATION Spec
CONSTANTS
  Speeds = {"one", "v", "w"}
  MaxFight = 2
  BuggyNoOpinionLifecycle = FALSE
  BuggyLifecyclePersist = TRUE
PROPERTIES
  PersistencePurity
